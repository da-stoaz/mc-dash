// Allow side-effect imports of global stylesheets, e.g. `import './globals.css'`.
// Next.js processes these at build time; this declaration keeps the TypeScript
// language server (and `tsc --noEmit`) from flagging the import in the editor.
declare module '*.css';
